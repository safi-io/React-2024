function rulesJSX() {
    return( 
        /* Rules
        1. Return a single root element (Can use DIV)
        2. Every Tag must be Closed
        3. Camel Case most of the time
        4. Can't use Class, must use className
        */
       <p>Hello World from Rules of JSX</p>
    )
}

export default rulesJSX;
